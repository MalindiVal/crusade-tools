////
// DRAW
////

// Background.
if ( mode == 0 )
{
	texture_set_interpolation ( 1 );
	stage_bg_projection ( 1 , 1280*(1072/720) , 1072 );
	draw_background ( background_index[0] , 0 , 0 );

	
	d3d_set_projection_ortho ( view_xview[0] , view_yview[0] , view_wview[0] , view_hview[0] , view_angle[0] );  
  
  
	texture_set_interpolation ( global.dinterpolation );
  
	/*
	texture_set_interpolation ( global.dinterpolation );
	

	draw_set_blend_mode ( bm_normal );

	var xx, yy, ww, hh, xxx, yyy;
	xx = view_xview[0];
	yy = view_yview[0];
	ww = view_wview[0];
	hh = view_hview[0];
		
	//draw_set_color ( c_white );
	//d3d_set_hidden ( 0 );
	
	//draw_set_color ( c_white );
	d3d_transform_set_identity();
	d3d_transform_add_rotation_z ( 0 );
	d3d_transform_add_rotation_x ( 90 );
	d3d_transform_add_rotation_y ( 0 );
	d3d_draw_ellipsoid(0,300,300,300,600,600,background_get_texture ( background_index[0] ),1,1,24);
	
		d3d_transform_set_identity();

	view_xview[0] = xx;
	view_yview[0] = yy;
	view_wview[0] = ww;
	view_hview[0] = hh;
	
	draw_set_blend_mode ( bm_normal );

	d3d_set_projection_ortho ( view_xview[0] , view_yview[0] , view_wview[0] , view_hview[0] , view_angle[0] );

	*/
		
	exit;
	
}