export interface FighterInfo {
    name:string;
    folder:string;
}

export interface StageInfo {
    name:string;
    folder:string;
}

export interface ItemInfo {
    name:string;
    folder:string;
}
